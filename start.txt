import network, time, urequests, json, socket, sys; [sys.modules.pop(m) for m in ['ssd1306','umqtt_simple','main'] if m in sys.modules]; w=network.WLAN(network.STA_IF); w.active(True); w.connect('Wokwi-GUEST',''); time.sleep(2); f=open('ssd1306.py','w'); f.write("import time\nfrom machine import Pin\nclass SSD1306:\n def __init__(self,w,h):\n  self.width=w;self.height=h;self.pages=h//8;self.buffer=bytearray(self.pages*w);self.init_display()\n def init_display(self):\n  for c in (0xAE,0xD5,0x80,0x20,0x00,0x21,0,self.width-1,0x22,0,self.pages-1,0xC8,0x40,0xA1,0xA6,0x81,0xCF,0x8D,0x14,0xAF):self.write_cmd(c)\n  self.fill(0);self.show()\n def poweroff(self):self.write_cmd(0xAE)\n def poweron(self):self.write_cmd(0xAF)\n def contrast(self,c):self.write_cmd(0x81);self.write_cmd(c)\n def invert(self,i):self.write_cmd(0xA6|(i&1))\n def show(self):self.write_data(self.buffer)\n def fill(self,c):\n  for i in range(len(self.buffer)):self.buffer[i]=0xFF if c else 0x00\n def pixel(self,x,y,c):\n  if 0<=x<self.width and 0<=y<self.height:\n   idx=(y//8)*self.width+x;off=y%8\n   if c:self.buffer[idx]|=(1<<off)\n   else:self.buffer[idx]&=~(1<<off)\n def text(self,s,x,y,c=1):\n  import framebuf;fbuf=framebuf.FrameBuffer(self.buffer,self.width,self.height,framebuf.MONO_VLSB);fbuf.text(s,x,y,c)\nclass SSD1306_I2C(SSD1306):\n def __init__(self,w,h,i2c,addr=0x3C):self.i2c=i2c;self.addr=addr;self.temp=bytearray(2);self.write_list=[b'\\x40', None];super().__init__(w,h)\n def write_cmd(self,c):self.temp[0]=0x00;self.temp[1]=c;self.i2c.writeto(self.addr,self.temp)\n def write_data(self,b):self.write_list[1]=b;self.i2c.writevto(self.addr,self.write_list)\n"); f.close(); f=open('umqtt_simple.py','w'); f.write("import usocket as socket\nimport ustruct as struct\nclass MQTTException(Exception):pass\nclass MQTTClient:\n def __init__(self,c,s,p=1883):self.client_id=c;self.sock=None;self.server=s;self.port=p;self.pid=0;self.cb=None\n def _send_str(self,s):self.sock.write(struct.pack('!H',len(s)));self.sock.write(s)\n def _recv_len(self):\n  n=0;sh=0\n  while 1:\n   b=self.sock.read(1)[0];n|=(b&0x7F)<<sh\n   if not b&0x80:return n\n   sh+=7\n def set_callback(self,f):self.cb=f\n def connect(self,clean=True):self.sock=socket.socket();self.sock.connect(socket.getaddrinfo(self.server,self.port)[0][-1]);m=bytearray([0x10,12+len(self.client_id),0,4,77,81,84,84,4,clean<<1,0,0]);self.sock.write(m);self._send_str(self.client_id);res=self.sock.read(4);return res[3]&1\n def disconnect(self):self.sock.write(b'\\xe0\\0');self.sock.close()\n def publish(self,t,m,retain=False,qos=0):self.sock.write(bytearray([0x30,2+len(t)+len(m)]));self._send_str(t);self.sock.write(m)\n def subscribe(self,t,qos=0):self.pid+=1;self.sock.write(bytearray([0x82,len(t)+5]));self.sock.write(struct.pack('!H',self.pid));self._send_str(t);self.sock.write(bytes([qos]))\n def check_msg(self):\n  self.sock.setblocking(False)\n  try:res=self.sock.read(1)\n  except OSError:return None\n  self.sock.setblocking(True)\n  if res is None or res==b'' or res==b'\\xd0':return None\n  if res[0]&0xF0!=0x30:return None\n  sz=self._recv_len();tl=struct.unpack('!H',self.sock.read(2))[0];t=self.sock.read(tl);sz-=tl+2\n  m=self.sock.read(sz);self.cb(t,m)\n"); f.close(); codigo="import machine, dht, time, network, urequests, json, socket, framebuf\nfrom machine import Pin, SoftI2C, PWM, ADC\nimport ssd1306\nimport umqtt_simple\nMQTTClient = umqtt_simple.MQTTClient\ni2c = SoftI2C(scl=Pin(22), sda=Pin(21))\noled = ssd1306.SSD1306_I2C(128, 64, i2c)\nsensor_dht = dht.DHT22(Pin(15))\nldr = ADC(Pin(34)); ldr.atten(ADC.ATTN_11DB)\nmq2 = ADC(Pin(35)); mq2.atten(ADC.ATTN_11DB)\ntrig = Pin(5, Pin.OUT); echo = Pin(18, Pin.IN)\nled_red = Pin(2, Pin.OUT); led_yellow = Pin(4, Pin.OUT); led_blue = Pin(16, Pin.OUT)\nservo = PWM(Pin(17), freq=50)\ndef get_dist():\n trig.value(0); time.sleep_us(2); trig.value(1); time.sleep_us(10); trig.value(0)\n t0=time.ticks_us()\n while echo.value()==0:\n  if time.ticks_diff(time.ticks_us(),t0)>10000: return 0\n t1=time.ticks_us()\n while echo.value()==1:\n  if time.ticks_diff(time.ticks_us(),t1)>25000: return 0\n return (time.ticks_diff(time.ticks_us(),t1)/2)/29.1\ndef get_weather():\n try:\n  r = urequests.get('http://api.openweathermap.org/data/2.5/weather?q=Mogi%20das%20Cruzes,BR&appid=4ef098a33622c042a0bc81f164d96554&units=metric')\n  t = r.json()['main']['temp']; r.close(); return t\n except: return 'Erro'\ndef sub_cb(topic, msg):\n c = msg.decode('utf-8')\n if c=='vermelho_on': led_red.value(1)\n elif c=='vermelho_off': led_red.value(0)\n elif c=='amarelo_on': led_yellow.value(1)\n elif c=='amarelo_off': led_yellow.value(0)\n elif c=='azul_on': led_blue.value(1)\n elif c=='azul_off': led_blue.value(0)\n elif c=='porta_abrir': servo.duty(115)\n elif c=='porta_fechar': servo.duty(40)\noled.fill(0); oled.text('Conectando...',0,0); oled.show()\nw=network.WLAN(network.STA_IF); w.active(True); w.connect('Wokwi-GUEST','')\nwhile not w.isconnected(): time.sleep(0.5)\nprint('IP Local:', w.ifconfig()[0])\nclima_mogi = get_weather()\nclient = MQTTClient('esp32_mogi_jcmcs_99', 'broker.hivemq.com')\nclient.set_callback(sub_cb)\ntry:\n client.connect(); client.subscribe(b'residencia/atuadores')\nexcept: pass\ns = socket.socket(socket.AF_INET, socket.SOCK_STREAM); s.bind(('', 80)); s.listen(5); s.setblocking(False)\ndef web_page():\n return '<html><head><style>.b{padding:10px;margin:5px;}</style></head><body><h1>ESP32 Mogi</h1><a href=\"/?led_red=on\"><button class=\"b\">Gas ON</button></a><a href=\"/?led_red=off\"><button class=\"b\">Gas OFF</button></a><br><a href=\"/?led_yellow=on\"><button class=\"b\">Sala ON</button></a><a href=\"/?led_yellow=off\"><button class=\"b\">Sala OFF</button></a><br><a href=\"/?led_blue=on\"><button class=\"b\">Ar ON</button></a><a href=\"/?led_blue=off\"><button class=\"b\">Ar OFF</button></a><br><a href=\"/?servo=open\"><button class=\"b\">Porta Abrir</button></a><a href=\"/?servo=close\"><button class=\"b\">Porta Fechar</button></a></body></html>'\nlast_pub = time.ticks_ms()\nwhile True:\n try:\n  client.check_msg()\n  try:\n   conn, addr = s.accept(); req = conn.recv(1024).decode('utf-8')\n   if '/?led_red=on' in req: led_red.value(1)\n   elif '/?led_red=off' in req: led_red.value(0)\n   elif '/?led_yellow=on' in req: led_yellow.value(1)\n   elif '/?led_yellow=off' in req: led_yellow.value(0)\n   elif '/?led_blue=on' in req: led_blue.value(1)\n   elif '/?led_blue=off' in req: led_blue.value(0)\n   elif '/?servo=open' in req: servo.duty(115)\n   elif '/?servo=close' in req: servo.duty(40)\n   conn.send('HTTP/1.1 200 OK\\nContent-Type: text/html\\nConnection: close\\n\\n' + web_page()); conn.close()\n  except OSError: pass\n  if time.ticks_diff(time.ticks_ms(), last_pub) >= 5000:\n   sensor_dht.measure(); t = sensor_dht.temperature(); u = sensor_dht.humidity(); l = ldr.read(); g = mq2.read(); d = get_dist()\n   oled.fill(0)\n   fb = framebuf.FrameBuffer(oled.buffer, 128, 64, framebuf.MONO_VLSB)\n   fb.fill_rect(0, 0, 128, 12, 1)\n   fb.text('MOGI: {}C'.format(clima_mogi), 16, 2, 0)\n   fb.vline(64, 12, 38, 1)\n   fb.text('T:{}C'.format(t), 4, 18, 1)\n   fb.text('U:{}%'.format(u), 4, 34, 1)\n   fb.text('G:{}'.format(g), 70, 18, 1)\n   fb.text('L:{}'.format(l), 70, 34, 1)\n   fb.hline(0, 50, 128, 1)\n   fb.text('DIST: {:.1f}cm'.format(d), 12, 54, 1)\n   oled.show()\n   client.publish(b'residencia/sensores', json.dumps({'temp': t, 'umid': u, 'luz': l, 'gas': g, 'dist': d}))\n   print('PUB OK'); last_pub = time.ticks_ms()\n  time.sleep_ms(10)\n except Exception as e:\n  print('Erro:', e); time.sleep(2)\n  try: client.connect(); client.subscribe(b'residencia/atuadores')\n  except: pass\n"; f=open('main.py','w'); f.write(codigo); f.close(); exec(open('main.py').read())